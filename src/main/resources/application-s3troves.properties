# S3-backed trove data. Set MOOCHO_BUCKET_NAME in the environment.
# Use with dev (H2 + classpath) or postgres (Postgres); e.g. SPRING_PROFILES_ACTIVE=postgres,s3troves
moocho.bucket.name=${MOOCHO_BUCKET_NAME:}
