#!/usr/bin/env bash
set -euo pipefail
# HTTP CLI for the Morsor backend (Python stdlib). See: tools/morsor-cli/morsor_cli.py
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec python3 "${ROOT}/tools/morsor-cli/morsor_cli.py" "$@"
