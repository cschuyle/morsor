# Profile no-auth: Spring Security permits all requests (no form login / CSRF / API tokens).
# Use only in trusted local environments. Combine with dev, s3troves, etc. as needed.
# Example: SPRING_PROFILES_ACTIVE=dev,no-auth
# Implemented by NoAuthSecurityConfig (replaces the normal SecurityConfig filter chain).
